# 1　模型只会说话，干不了活

**—— 大模型的边界**

你让 AI 帮你写过东西。可能是一段文案，可能是一份表格的内容，也可能是一小段代码。

写完之后有一步是你自己做的：把它复制出来，放到该放的地方。

这一步看着不起眼。可只要你想让 AI 做一件稍微大一点的事，问题就出来了。

比如你想做一个真的能打开、能用的小工具。AI 给你的东西要分别放进好几个文件，还得按顺序放对；放完要运行一下看行不行；不行还得把哪里出错了原样告诉它；它改完，你再放一遍，再运行一遍。

来回七八轮之后你会发现，这里面真正需要"聪明"的部分很少，绝大多数时间你在当搬运工。

搬运的是你，动脑的是它。而它连自己写出来的东西被放到哪儿了都不知道。

## 先说清楚这本书讲的东西能干什么

免得你读到一半发现讲的不是你要的。

DeepSeek Harness 给 AI 的能力，拆开只有三样：

**读和写你电脑上的文件。** 打开一个文件看内容，改其中几行，新建文件，在一堆文件夹里按名字或者按内容找东西。

**在你电脑上执行命令。** 就是那种在黑底白字的窗口里敲一行、按回车、然后有事情发生的操作。

**联网搜索和抓网页。**

就这三样。所有别的本事都是拿这三样拼出来的。

写代码正好是这三样的组合：改代码是写文件，跑测试是执行命令。所以它天然最适合干这个。官方的定位也很明确，界面上四种运行模式里，有两种的说明里直接写着"编码 Agent"。

但凡是能拆成这三样的事，它同样能做：

- 把三百个命名混乱的文件按规则重命名
- 把二十份表格合并成一张，顺便去掉重复行
- 从一堆文档里把关键信息挑出来汇总成清单
- 按一个模板批量生成一百份文件
- 抓一批网页存到本地整理成能读的格式

**有一件事它做不了：它不会替你操作软件。**

你让它做个 PPT，它不会去打开 PowerPoint 一页页点菜单、拖文本框。它没有鼠标，看不见你的屏幕。

它能做的是写一段代码，让电脑生成一个 .pptx 文件，前提是你机器上装了能干这事的工具。生成出来的东西打开能用，但它整个过程都没"看见"过那份 PPT 长什么样。

所以边界大致是这样：

> 它擅长跟文件和命令打交道，不擅长跟屏幕上的界面打交道。

## 文字进，文字出

大语言模型的构造说到底是一件很单纯的事：你给它一段文字，它接着往下写一段文字。

它写出来的东西之所以有用，是因为训练时读过海量文本，学会了"在这样一段文字后面，接什么最合理"。你问它"这段报错是什么意思"，最合理的续写就是一段解释。你说"帮我改一下"，最合理的续写就是一段改好的代码。

这个机制很强大，边界也由同一件事定死：它的输出只有文字。

文字打不开文件。文字不能在你的电脑上执行命令。文字不能把结果拿回来。

它写出"我建议你把第 14 行改成这样"，这句话本身没有任何力量去改动那第 14 行。改动是你手动完成的。

这个边界有多硬，可以直接去查。DeepSeek 官方接口文档里，`deepseek-v4-flash` 和 `deepseek-v4-pro` 这两个型号能接收的输入只有文字一种。在 DeepSeek Harness 的代码里，这件事被写死在 `packages/llm/llm-deepseek/src/adapter.ts` 的第 113 行，那一行只有一个词：text。

同一个目录下另一个文件专门负责在有人试图塞图片进来时报错，错误信息写着"DeepSeek 的这条通道不支持图片内容"。

## 它其实不记得你

用过聊天框的人通常没意识到另一件事。

你跟 AI 聊了十轮，第十一轮它还能接得上前面的话。看起来它记得你们聊过什么。

它不记得。

每次你发一句话，聊天软件实际发过去的不只是这一句，而是把前面十轮的全部内容重新发一遍，加上你的新问题，打包成一大段文字送过去。模型每次都是第一次见到这些内容。它没有记忆，只是每次都被重新告知了一遍。

这种"自己不存任何东西"的特性叫**无状态**。

无状态带来一个直接后果：这段历史越聊越长，而模型一次能读的字数有上限。聊到某个长度，前面的内容必须被丢掉一部分。这件事该怎么丢、丢哪些、丢之前要不要先留个摘要，模型自己完全管不了。

## 它看不见，所以它会猜

你问它"我这个项目里配置文件在哪"，它没法去你电脑上看一眼。它只能根据"一般的项目长什么样"来推测，然后给你一个听上去很合理的答案。

推测出来的路径可能根本不存在。

它不是在骗你。在它那套机制里，"给出一个最像正确答案的续写"和"给出正确答案"是同一件事，因为它没有任何办法去核对。这种一本正经地说出不存在的东西，叫**幻觉**。

幻觉的成因不止一种，但上面这一种很好理解：它够不着真实世界，只能猜。给它一个能真的去看一眼的办法，这一类猜错就不会发生。

## 那几年，人就是那层马具

回头看 2022 到 2023 年，大部分人用 AI 写代码，就是开头那个复制粘贴的循环。

在那个循环里，你替它做了四件事：

**你是它的眼睛。** 它看不见你的代码，所以你把代码贴进去给它看。贴哪一段、贴多少，你决定。

**你是它的手。** 它改不了文件，所以它写出来的代码由你复制、粘贴、保存。

**你是它的跑腿。** 它运行不了程序，所以你去点运行，把报错原样带回来给它。

**你是它的记事本。** 聊太长了它会丢掉前面的内容，所以你得判断哪些关键信息要重新贴一遍。

这四件事加起来占了整个过程的绝大部分时间。真正需要动脑的那一步——想清楚这个 bug 是怎么回事——反而最快。

而这四件事每一件都是机械的。贴代码、存文件、点运行、翻聊天记录，全是有固定套路、可以照着规则重复做的动作。

凡是机械的，就能交给程序。

## 把这四件事交出去

一旦有一段程序替你做完这四件事，那个循环就不需要人守着了。

下面是一次真实运行的记录。文件夹里有九个乱七八糟的文件：两张图片、两份表格、四个文档、一个压缩包。给它的指令是一句话：

> 把当前目录里的文件按类型整理到子文件夹：图片放 images，表格放 data，文档放 docs，其他放 misc。

它做的第一件事不是搬文件，是先跑了一条命令要目录清单：

```
ls -la
```

拿到清单之后，它拼出了第二条命令：

```
mkdir -p images data docs misc && mv banner.png logo.png images/ && mv 报表2024.csv 报表2025.csv data/ && mv 会议记录.txt 会议记录2.txt 草稿.md 草稿v2.md docs/ && mv 备份.zip misc/ && ls -la
```

搬完之后它又跑了第三条，挨个文件夹点了一遍数：

```
for d in images data docs misc; do echo "== $d =="; ls "$d"; done
```

九个文件全部归位。整个过程 8.3 秒，三次工具调用。

这里有个地方值得停一下：**它没有"整理文件"这个功能。**

它手上只有一个能力——能敲命令。上面那条把 mkdir 和四个 mv 串起来的命令，是它当场拼出来的。

这也是为什么这本书讲的东西名义上是"编程工具"，却跟不写代码的人有关系：**你交给它的每一件杂活，它都是靠现场写出一条命令去完成的。** 用它干活和它是个编程工具，本来就是同一件事。

那段替你当眼睛、当手、当跑腿、当记事本的程序，它一个决定都不做——改哪一行、怎么改，全是模型说的。它干的是搬运的活。

这层东西有个专门的名字，来自马身上那套装备。下一篇讲它。
