# 2　马有的是力气，可它拉不动车

**—— Harness**

马有的是力气。可一匹什么都没套的马拉不动车，它不知道该往哪边走，使出来的力气也没地方传出去。

缺的是套在它身上、把它和车连起来的那一整套东西：缰绳、皮带、颈圈。中文一般管这叫**马具**。

马具解决的不是力气问题，是把力气变成"朝着一个方向，拉动一样具体的东西"。

把这个画面挪到 AI 上。

大语言模型就是那匹马。它能听懂你说的话，能推理，能写出正确的代码。但它自己什么也干不了，因为它的输出只有一种形式：文本。它打不开你电脑上的文件，执行不了任何一条命令，也记不住上一次跟你说过什么。

要让模型真的干活，得在它外面套一层：把它想做的事翻译成真实动作，做完把结果送回去，顺便把整个过程记下来。这一层就叫 harness，英文里正是"马具"这个词。

DeepSeek 官方文档里写的公式是这个：

```
Model + Harness = Agent
```

Harness 团队负责人崔添翼在 2026 年 6 月 21 日一条公开招聘的推文里说得更直白：Agent 是汽车，模型是发动机，Harness 是方向盘、变速箱、刹车。

## 这一层每天在干什么

### 一、把工具递给模型

模型只会输出文本，所以"读一个文件"这件事的真实流程有三步。

第一步，harness 先告诉模型：你可以用一个叫"读文件"的工具，用的时候要给出文件路径。

第二步，模型输出一段文本，意思是"我要用读文件这个工具，路径是 src/index.ts"。

第三步，harness 看懂这段文本，真的去硬盘上找到那个文件，把里面的内容送回给模型。

整个过程里，模型从头到尾没碰过你的电脑。碰电脑的是 harness。

DeepSeek Harness 给模型准备了 51 个工具。有读文件的，有改文件的，有在整个项目里按名字或者按内容搜的，有直接在你电脑上执行命令的，有开一个终端窗口并且一直挂着的，有联网搜索的，还有一个是"叫另一个 AI 来接手这一段活"。完整清单在项目文档的 `docs/tool-catalog.md` 里。

（项目文档在哪：DeepSeek 把这个软件的全部代码和文档都放在 GitHub 上，网址是 github.com/deepseek-ai/deepseek-harness。这种放代码的地方叫仓库，任何人都能打开看，不用注册。）

### 二、把一次对话变成一串动作

修一个 bug 很少能一步做完。常见的过程是这样的：改一行代码，跑一次测试，看到报错，再改一行，再跑一次，来回十几轮才修好。

模型每次只能回一句话。把这些话连成一个连续过程的是 harness：它拿到模型的回复，执行模型要求的工具，把结果送回去，再问下一步。什么时候该停下来，也是它判断的。

这个来回过程在 DeepSeek Harness 里有两个名字。你说一句话，它一直忙到彻底停下来，这一整段叫一个 **turn**。中间它每问一次模型、再执行一批工具，这一小轮叫一个 **step**。

上面那个修 bug 的例子里，你只说了一句"把登录那个 bug 修了"，这是一个 turn。它中间改了十几次、跑了十几次测试，那是十几个 step。

分成两层是有用处的。你中途插一句"等等，先别动数据库"，harness 得判断这句话是打断眼下这一小步，还是等整件事做完再处理。这两种处理方式在代码里是分开写的。

完整流程写在项目文档 `docs/architecture.md` 的第 67 行，是一段十几行的伪代码，从 turn/start 开始，到 turn/end 结束。

### 三、管住那张桌子

模型一次能读多少字，是有上限的。超过了，前面说过的话就得挤掉一部分。这个上限叫**上下文窗口**。

可以把它想成一张桌子。桌面就这么大，新材料铺上来，旧的就得收走。一个几十万行的代码库摆不上这张桌子，聊上两个小时的对话也会摆不下。

决定哪些留在桌上、哪些收走、收走之前要不要先抄个摘要贴张便利贴、哪些东西太大干脆塞进抽屉只在桌面留张条，这些全是 harness 的活。

### 四、记账

DeepSeek Harness 把每一次交互都写进一条只能往后追加、不能回头修改的日志。你说的每句话、模型回的每个字、每次用了什么工具、结果是什么，全在里面。

发给模型的对话历史不单独存一份，而是每次从这条日志现算出来的。

这条规矩在项目里被写成一句话：**模型可见即已记录。** 任何能进入模型视野的东西，都必须能从日志里重建出来。代码里埋了一处检查，一旦发现有东西没进日志就当场报错，不让程序继续跑，这种检查叫**断言**。

好处是三件事变成白送的：可以翻回到任何一步重新开始，可以从中间岔出去试另一条路而不影响原来那条，也可以把整个过程从头播一遍，看当时到底发生了什么。

## 一次完整的来回长什么样

给它一句话：

> 把当前目录里的文件按类型整理到子文件夹：图片放 images，表格放 data，文档放 docs，其他放 misc。

文件夹里有九个乱七八糟的文件：两张图、两份表格、四个文档、一个压缩包。

**第 1 步。** harness 把这句话，连同全部工具的说明书，一起发给模型。模型回来的第一个动作不是动手，是先跑了一条命令：

```
ls -la
```

它没有直接开始搬文件，而是先要了一份目录清单。

**第 2 步。** 看到目录内容之后，模型拼出了一条命令：

```
mkdir -p images data docs misc && mv banner.png logo.png images/ && mv 报表2024.csv 报表2025.csv data/ && mv 会议记录.txt 会议记录2.txt 草稿.md 草稿v2.md docs/ && mv 备份.zip misc/ && ls -la
```

harness 执行了它，把结果送回去。

**第 3 步。** 模型没有直接收工，而是又要了一条命令，挨个文件夹点数：

```
for d in images data docs misc; do echo "== $d =="; ls "$d"; done
```

**第 4 步。** 四个文件夹的内容都对上了，模型这次不再要任何工具，只回了一张整理结果的表格。没有工具要执行，harness 判断这件事做完了，停下来等下一句话。

全程 8.3 秒，3 次工具调用，4 个 step，1 个 turn。

这里有个细节值得停一下：**它没有"整理文件"这个功能。** 它有的只是"能敲命令"这一个能力，上面那条 mkdir 加四个 mv 串起来的命令，是它现场拼的。

## 这个词是什么时候开始用的

Harness 在软件行业不是新词。做软件测试的人几十年前就在用 test harness，指的是把一段代码套起来、喂给它输入、看它吐出来的东西对不对的那套外壳。

这个词进 AI 圈，比较早的一次是 2020 年 8 月 28 日。一个叫 EleutherAI 的开源团队建了个工具叫 lm-evaluation-harness，把模型套起来跑考试题，看它能考多少分。

2023 年那批工具出来的时候，同一样东西大家管它叫 scaffolding，脚手架。AutoGPT 是那年 3 月，Aider 是 5 月，都是能自己动手写代码的开源工具。

两个词说的是一回事，暗示的东西不一样。脚手架是临时的，楼盖完就拆掉。马具是长期的，只要还用这匹马，就得一直套着。

行业从"脚手架"改口叫 harness，说明这层东西的地位变了。它从一个凑合用的外部支撑，变成了拆不掉的传动装置。

目前能查到最早写下 agent harness 这个说法的，是 2024 年 12 月的一篇论文。到 2026 年初，harness engineering 才作为一门独立手艺被提出来，谁最先提的还有争议。

## 为什么这个词现在才出现

模型不够强的时候，harness 写成什么样区别不大。瓶颈在模型身上，外面这层再精巧，模型该犯的错还是会犯。那个阶段没人需要一个专门的词来讨论它。

2024 年出现了一个转折。有人收集了一批真实项目里的 bug，做成一套题库专门考 AI 能修好几道，这套题叫 SWE-bench。大家发现同一个模型，换一套外壳，能修好的题目数量差出一大截。

模型没变，分数变了。变量落在了外面那一层。

到 2026 年，Claude Code 和 Codex 这类工具用的是同代水平的模型，好不好用主要看各自的 harness 写得怎么样。

一件事只有变成了变量，才值得被命名。
